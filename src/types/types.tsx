export interface EmployeeType {
    id: number;
    email: string;
    firstName: string;
    lastName: string;
}

export interface EmployeeStateType {
    firstName: string;
    lastName: string;
    email: string
}